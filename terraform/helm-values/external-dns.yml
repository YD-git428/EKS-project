provider: aws 

env: 
- name: AWS_DEFAULT_REGION
  value: $REGION

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: $EXT_DNS_IAM_ROLE

domainFilters:
- youcefderder.co.uk #Insert your own domain